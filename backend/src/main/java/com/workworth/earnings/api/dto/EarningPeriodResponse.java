package com.workworth.earnings.api.dto; import com.workworth.earnings.domain.EarningPeriodSummary; import java.math.BigDecimal; import java.time.LocalDate; public record EarningPeriodResponse(String context,LocalDate startDate,LocalDate endDate,String status,BigDecimal amount,String currencyCode){public static EarningPeriodResponse from(EarningPeriodSummary s){return new EarningPeriodResponse(s.period().name(),s.startDate(),s.endDate(),s.status().name(),s.publicAmount(),s.currencyCode());}}
